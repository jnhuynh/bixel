<table class='table table-stripe'd>
  <tr>
    <th>Name</th>
    <th><th>
  </tr>
  {{#each level in levels}}
    <tr>
      <td>{{level.name}}</td>
      <td>
        <button type='submit' class='btn btn-primary' {{action enterLevel level}}>Play</button>
      </td>
    </tr>
  {{/each}}
</table>
