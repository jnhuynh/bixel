<h1>{{currentLevel.name}}: {{currentPlayer.name}}</h1>
<h1>{{currentPlayer.direction}}({{currentPlayer.x}},{{currentPlayer.y}})</h1>
{{view 'game' levelBinding='currentLevel'  currentPlayerBinding='currentPlayer' dispatcherBinding='dispatcher' updateChannelBinding='updateChannel'}}
